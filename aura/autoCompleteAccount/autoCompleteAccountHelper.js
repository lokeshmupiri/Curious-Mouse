({
	autoComplete : function(cmp) {
		var self = this;
        var keyPresses = Rx.Observable.fromEvent(cmp.find('atcmptbox').getElement(),'keyup');
        function getAccounts(cmp){
            
        }
	}
})